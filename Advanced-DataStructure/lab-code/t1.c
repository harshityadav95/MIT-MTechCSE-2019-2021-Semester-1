#include<stdio.h>
#include<stdlib.h>
int main()
{
	int v;
	int a[20][20],k=0,edge[20][20];
	scanf("%d",&v);
	for (int i = 0; i < v; ++i)
	{
		for (int j = 0; j < v; ++j)
		{
			scanf("%d",&a[i][j])
			if (a[i][j]!=0)
			{
				
			}
		}
	}

	return 0;
}